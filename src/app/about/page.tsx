import AboutCarousel from "@/components/about/AboutCarousel";

export const metadata = {
  title: "About — UniHero",
  description: "UniHero timeline and milestones",
};

export default function AboutPage() {
  return <AboutCarousel />;
}
